<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xgb.dao.SysPermissionSqlMapper">

    <sql id="Base_Column_List">
            sp.ID AS id,
            sp.PERMISSION_NAME AS permissionName,
            sp.PERMISSION_KEY AS permissionKey,
            sp.PARENT_ID AS parentId,
            sp.PERMISSION_CODE AS permissionCode,
            sp.STATUS AS status,
            sp.REMARK AS remark,
            sp.CREATE_TIME AS createTime,
            sp.CREATE_ID AS createId,
            sp.UPDATE_TIME AS updateTime,
            sp.UPDATE_ID AS updateId,
            sp.PERMISSION_TYPE AS permissionType

    </sql>

    <select id="getSysPermissionCount" resultType="java.lang.Integer" parameterType="Map">
        select count(*) from SYS_PERMISSION sp
        where 1=1
       
    </select>


    <select id="getSysPermissionList" resultType="com.xgb.model.SysPermission" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_PERMISSION sp
        where 1=1
        
        <if test="begin!=null and end != null">
            limit #{begin},#{end}
        </if>
    </select>
    <select id="selectOne" resultType="com.xgb.model.SysPermission" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_PERMISSION sp
        where 1=1
        
        limit 0,1
    </select>

    <select id="selectByParentId" resultType="com.xgb.model.vo.SysPermissionVO" parameterType="java.lang.String">
        select
        <include refid="Base_Column_List"/>
        from SYS_PERMISSION sp
        where sp.PARENT_ID = #{parentId}
    </select>
</mapper>