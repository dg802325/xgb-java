<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xgb.dao.SysLogSqlMapper">

    <sql id="Base_Column_List">
            tn.ID AS id,
            tn.PARAM_KEY AS paramKey,
            tn.PARAM_VALUE AS paramValue,
            tn.STATUS AS status,
            tn.REMARK AS remark,
            tn.CREATE_TIME AS createTime,
            tn.UPDATE_TIME AS updateTime,
            tn.CREATE_ID AS createId,
            tn.UPDATE_ID AS updateId
            tn.ID AS id,
            tn.PARENT_ID AS parentId,
            tn.DEPT_NAME AS deptName,
            tn.SORT AS sort,
            tn.STATUS AS status,
            tn.CREATE_TIME AS createTime,
            tn.UPDATE_TIME AS updateTime,
            tn.CREATE_ID AS createId,
            tn.UPDATE_ID AS updateId
            tn.ID AS id,
            tn.DICT_NAME AS dictName,
            tn.DICT_TYP AS dictTyp,
            tn.DICT_CODE AS dictCode,
            tn.DICT_VALUE AS dictValue,
            tn.SORT AS sort,
            tn.REMARK AS remark,
            tn.STATUS AS status,
            tn.CREATE_TIME AS createTime,
            tn.UPDATE_TIME AS updateTime,
            tn.CREATE_ID AS createId,
            tn.UPDATE_ID AS updateId
            tn.ID AS id,
            tn.USER_NAME AS userName,
            tn.OPERATION AS operation,
            tn.METHOD AS method,
            tn.PARAMS AS params,
            tn.LOG_TIME AS logTime,
            tn.IP AS ip,
            tn.CREATE_TIME AS createTime
    </sql>

    <select id="getSysLogCount" resultType="java.lang.Integer" parameterType="Map">
        select count(*) from SYS_LOG sl
        where 1=1
       
        <if test="userName!=null and userName!=''">
            AND sl.USER_NAME = #{userName}
        </if>

        <if test="method!=null and method!=''">
            AND sl.METHOD = #{method}
        </if>

        <if test="logTime!=null and logTime!=''">
            AND sl.LOG_TIME = #{logTime}
        </if>

        <if test="createTime!=null and createTime!=''">
            AND sl.CREATE_TIME = #{createTime}
        </if>
        order by sl.ID asc
    </select>


    <select id="getSysLogList" resultType="com.xgb.model.SysLog" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_LOG sl
        where 1=1
        
        <if test="userName!=null and userName!=''">
            AND sl.USER_NAME = #{userName}
        </if>

        <if test="method!=null and method!=''">
            AND sl.METHOD = #{method}
        </if>

        <if test="logTime!=null and logTime!=''">
            AND sl.LOG_TIME = #{logTime}
        </if>

        <if test="createTime!=null and createTime!=''">
            AND sl.CREATE_TIME = #{createTime}
        </if>
        order by sl.ID asc
        <if test="begin!=null and end != null">
            limit #{begin},#{end}
        </if>
    </select>
    <select id="selectOne" resultType="com.xgb.model.SysLog" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_LOG sl
        where 1=1
        
        <if test="userName!=null and userName!=''">
            AND sl.USER_NAME = #{userName}
        </if>

        <if test="method!=null and method!=''">
            AND sl.METHOD = #{method}
        </if>

        <if test="logTime!=null and logTime!=''">
            AND sl.LOG_TIME = #{logTime}
        </if>

        <if test="createTime!=null and createTime!=''">
            AND sl.CREATE_TIME = #{createTime}
        </if>
        order by sl.ID asc
        limit 0,1
    </select>

</mapper>