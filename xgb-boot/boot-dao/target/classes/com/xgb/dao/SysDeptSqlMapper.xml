<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xgb.dao.SysDeptSqlMapper">

    <sql id="Base_Column_List">
            tn.ID AS id,
            tn.PARAM_KEY AS paramKey,
            tn.PARAM_VALUE AS paramValue,
            tn.STATUS AS status,
            tn.REMARK AS remark,
            tn.CREATE_TIME AS createTime,
            tn.UPDATE_TIME AS updateTime,
            tn.CREATE_ID AS createId,
            tn.UPDATE_ID AS updateId
            tn.ID AS id,
            tn.PARENT_ID AS parentId,
            tn.DEPT_NAME AS deptName,
            tn.SORT AS sort,
            tn.STATUS AS status,
            tn.CREATE_TIME AS createTime,
            tn.UPDATE_TIME AS updateTime,
            tn.CREATE_ID AS createId,
            tn.UPDATE_ID AS updateId
    </sql>

    <select id="getSysDeptCount" resultType="java.lang.Integer" parameterType="Map">
        select count(*) from SYS_DEPT sd
        where 1=1
       
    </select>


    <select id="getSysDeptList" resultType="com.xgb.model.SysDept" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_DEPT sd
        where 1=1
        
        <if test="begin!=null and end != null">
            limit #{begin},#{end}
        </if>
    </select>
    <select id="selectOne" resultType="com.xgb.model.SysDept" parameterType="Map">
        select
        <include refid="Base_Column_List"/>
        from SYS_DEPT sd
        where 1=1
        
        limit 0,1
    </select>

</mapper>